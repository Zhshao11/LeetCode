# 3.无重复字符的最长子串
## 属于不定长滑动窗口
- 同向双指针，从满足要求到不满足要求
- 统一格式：for-while-更新返回值
# 146.LRU缓存机制
[[基础算法精讲#LRU缓存]]
# 206.反转链表
## 递归（尾插法）
- 1.写出结束递归条件
- 2.递：一直递到链表末尾
- 3.归：在归的时候head.next就是新链表末尾，然后把当前节点插到末尾，尾部指向null
- 返回的是递归的值，也就是原链表的尾节点，最后递归完就是头节点了
## 迭代（头插法）
- 使用三个变量：prev、cur、nxt
  - prev为头结点，cur为遍历到的节点，nxt为cur下一个节点
  - 每次遍历把cur插到prev前面，然后给prev赋值为cur(当前头结点),cur赋值为nxt为下一次要遍历的节点
- 性质：在原链表中，prev指向尾节点，cur指向这段的下一个节点
  - 如果要反转中间链表：pO为需要反转的段的上一个节点：p0.next.next指向cur，p0.next指向prev;
### 92.反转链表 II
- 注意一开始将p0从dummy一到left-1的地方，循环从0开始，一直到left-2（即<left-1）
### 25.K个一组翻转链表
- 翻转涉及到的几个ListNode：
  - dummy:哨兵节点，dummy.next永远是头节点
  - p0:控制链表的连接，是翻转链表的上一个节点
  - pre:是上一个头节点，初始是null，后续被cur赋值
  - cur:当前要插入的节点，一开始是p0.next，后续被nxt = cur.next赋值
  - nxt:当前要插入的下一个节点，用于存下一个要插入的节点，用于给cur赋值
- 这道题在循环翻转时要判断剩余链表长度还够不够翻转，以及需要在连接链表之前使用n1来存p0.next赋值给p0，因为p0负责连接，需要跟着循环翻转过程动
# 215.数组中的第K个最大元素
- 要求：时间复杂度为O(n)
## 快速选择算法